.content {
    height: 100%;
    max-height: 812px;
    overflow-y: auto;
    width: 100%;
    padding-right: 8px;
    scrollbar-gutter: stable;
}

.list {
    list-style: none;
    margin: 0;
    padding: 0;
    display: flex;
    flex-direction: column;
    gap: 16px;
    overflow-y: auto;

}

.item { margin: 0; }

.link {
    color: inherit;
    text-decoration: none;
    display: block;
}

.content::-webkit-scrollbar { width: 8px; }
.content::-webkit-scrollbar-thumb { background: #2f2f37; border-radius: 8px; }
.content::-webkit-scrollbar-track { background: transparent; }
