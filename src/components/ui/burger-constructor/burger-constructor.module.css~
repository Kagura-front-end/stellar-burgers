:root{
    --tab-w: 536px;
    --row-h: 80px;
    --rail: 16px;
    --gap: 12px;
    --gutter: 16px;
    --grip-color: #F2F2F3;
}

.burger_constructor{
    inline-size: calc(var(--rail) + var(--tab-w) + var(--gap) + var(--gutter));
    display: flex;
    flex-direction: column;
    height: 664px;
    color: #f2f2f3;
    padding-bottom: 20px;
}

.scrollArea{
    inline-size: calc(var(--rail) + var(--tab-w) + var(--gap) + var(--gutter));
    max-block-size: 420px;
    overflow-y: auto;
    overflow-x: hidden;
    padding-inline-end: var(--gutter);
    box-sizing: border-box;
    scrollbar-gutter: stable both-edges;
}

.list{
    inline-size: calc(var(--rail) + var(--tab-w) + var(--gap));
    list-style: none;
    margin: 0;
    padding: 0;
}

.row{
    position: relative;
    padding-inline-start: calc(var(--rail) + var(--gap));
    margin-block: 8px;
    min-block-size: var(--row-h);
    display: flex;
    align-items: center;
}

.rowHandle{
    position: absolute;
    inset-inline-start: 0;
    inset-block: 0;
    inline-size: var(--rail);
    display: grid;
    place-items: center;
    padding: 0;
    margin: 0;
    border: 0;
    background: none;
    box-sizing: border-box;
    appearance: none;
    -webkit-appearance: none;
    cursor: grab;
}

.rowHandle:disabled{
    cursor: default;
}

.rowHandle::before{
    content: "";
    inline-size: 16px;
    block-size: 24px;
    display: block;
    background:
            radial-gradient(circle 2px, var(--grip-color) 99%, transparent 0) 4px 4px / 8px 8px no-repeat,
            radial-gradient(circle 2px, var(--grip-color) 99%, transparent 0) 12px 4px / 8px 8px no-repeat,
            radial-gradient(circle 2px, var(--grip-color) 99%, transparent 0) 4px 12px / 8px 8px no-repeat,
            radial-gradient(circle 2px, var(--grip-color) 99%, transparent 0) 12px 12px / 8px 8px no-repeat,
            radial-gradient(circle 2px, var(--grip-color) 99%, transparent 0) 4px 20px / 8px 8px no-repeat,
            radial-gradient(circle 2px, var(--grip-color) 99%, transparent 0) 12px 20px / 8px 8px no-repeat;
    opacity: .9;
}

.rowTab{
    inline-size: var(--tab-w);
    min-block-size: var(--row-h);
    box-sizing: border-box;
    background: transparent;
    border: 0;
    border-radius: 0;
}

.rowTab :global(.constructor-element){
    display: block;
    inline-size: 100%;
    margin: 0;
}

.edgeFrame{
    inline-size: calc(var(--rail) + var(--tab-w) + var(--gap) + var(--gutter)); /* 580 */
    padding-inline-end: var(--gutter);  /* same gutter as the scroll area */
    box-sizing: border-box;
}
