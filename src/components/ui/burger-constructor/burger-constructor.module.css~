:root {
    --tab-w: 536px;
    --row-h: 80px;
    --rail: 16px;
    --gap: 12px;
    --gutter: 16px;
    --grip-color: #F2F2F3;
    --grip-shift-x: -5px;
    --grip-shift-y: -2px;
    --grip-col-gap: 8px;
    --grip-row-gap: 8px;
    --grip-x0: 4px;
    --grip-y0: 4px;
}

.burger_constructor {
    inline-size: calc(var(--rail) + var(--tab-w) + var(--gap) + var(--gutter));
    display: flex;
    flex-direction: column;
    height: 664px;
    color: #f2f2f3;
    padding-bottom: 20px;
}

.scrollArea {
    inline-size: calc(var(--rail) + var(--tab-w) + var(--gap) + var(--gutter));
    max-block-size: 420px;
    overflow-y: auto;
    overflow-x: hidden;
    padding-inline-end: var(--gutter);
    box-sizing: border-box;
    scrollbar-gutter: auto;
}

.list {
    inline-size: calc(var(--rail) + var(--tab-w) + var(--gap));
    list-style: none;
    margin: 0;
    padding: 0;
}

.row {
    position: relative;
    padding-inline-start: calc(var(--rail) + var(--gap));
    margin-block: 8px;
    min-block-size: var(--row-h);
    display: flex;
    align-items: center;
}

.rowHandle {
    position: absolute;
    inset-inline-start: 0;
    inset-block: 0;
    inline-size: var(--rail);
    display: grid;
    place-items: center;
    padding: 0;
    margin: 0;
    border: 0;
    background: none;
    box-sizing: border-box;
    appearance: none;
    -webkit-appearance: none;
    cursor: grab;
}

.rowHandle:disabled {
    cursor: default;
}

.rowHandle::before {
    content: "";
    inline-size: 16px;
    block-size: 24px;
    display: block;
    opacity: .9;
    pointer-events: none;

    background: radial-gradient(circle 2px, var(--grip-color) 99%, transparent 0) calc(var(--grip-x0) + var(--grip-shift-x)) calc(var(--grip-y0) + var(--grip-shift-y)) / 8px 8px no-repeat,
    radial-gradient(circle 2px, var(--grip-color) 99%, transparent 0) calc(var(--grip-x0) + var(--grip-col-gap) + var(--grip-shift-x)) calc(var(--grip-y0) + var(--grip-shift-y)) / 8px 8px no-repeat,
    radial-gradient(circle 2px, var(--grip-color) 99%, transparent 0) calc(var(--grip-x0) + var(--grip-shift-x)) calc(var(--grip-y0) + var(--grip-row-gap) + var(--grip-shift-y)) / 8px 8px no-repeat,
    radial-gradient(circle 2px, var(--grip-color) 99%, transparent 0) calc(var(--grip-x0) + var(--grip-col-gap) + var(--grip-shift-x)) calc(var(--grip-y0) + var(--grip-row-gap) + var(--grip-shift-y)) / 8px 8px no-repeat,
    radial-gradient(circle 2px, var(--grip-color) 99%, transparent 0) calc(var(--grip-x0) + var(--grip-shift-x)) calc(var(--grip-y0) + var(--grip-row-gap) * 2 + var(--grip-shift-y)) / 8px 8px no-repeat,
    radial-gradient(circle 2px, var(--grip-color) 99%, transparent 0) calc(var(--grip-x0) + var(--grip-col-gap) + var(--grip-shift-x)) calc(var(--grip-y0) + var(--grip-row-gap) * 2 + var(--grip-shift-y)) / 8px 8px no-repeat;
}

.rowTab {
    inline-size: var(--tab-w);
    min-block-size: var(--row-h);
    box-sizing: border-box;
    background: transparent;
    border: 0;
    border-radius: 0;
}

.rowTab :global(.constructor-element) {
    display: block;
    inline-size: 100%;
    margin: 0;
}

.edgeFrame {
    inline-size: calc(var(--rail) + var(--tab-w) + var(--gap) + var(--gutter));
    padding-inline-end: var(--gutter);
    box-sizing: border-box;
}

.noBuns {
    box-sizing: border-box;
    min-height: 80px;
    padding: 16px 24px;
    display: flex;
    align-items: center;
    justify-content: center;
    border-radius: var(--common-border-radius-s);
    background: var(--background-element, #2f2f37);
}

.noBunsTop {
    border-radius: var(--top-constructor-item-border-radius);
}

.noBunsBottom {
    border-radius: var(--bottom-constructor-item-border-radius);
}

.total {
    display: flex;
    align-items: center;
    justify-content: flex-end;
    gap: 24px;
    width: 100%;
}

.cost {
    display: inline-flex;
    align-items: center;
    gap: 12px;
    line-height: 1;
}

.cost :global(svg) {
    min-width: 24px;
    min-height: 24px;
    width: 24px;
    height: 24px;
    flex-shrink: 0;
}

.rowTop .rowHandle,
.rowBottom .rowHandle {
    display: none;
}

.rowHandle:disabled::before {
    display: none;
}

.rowTab :global(svg) {
    min-width: 24px;
    min-height: 24px;
    width: 24px;
    height: 24px;
    flex-shrink: 0;
}

.cost .totalPrice {
    font-size: 30px;
    line-height: 36px;
}
