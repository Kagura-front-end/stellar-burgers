:root{
    --tab-w: 536px;
    --row-h: 80px;
    --rail: 16px;
    --gap: 12px;
    --gutter: 16px;
    --grip-color: #F2F2F3;
}

.burger_constructor{
    inline-size: calc(var(--rail) + var(--tab-w) + var(--gap) + var(--gutter));
    display: flex;
    flex-direction: column;
    height: 664px;
    color: #f2f2f3;
    padding-bottom: 20px;
}

.scrollArea{
    inline-size: calc(var(--rail) + var(--tab-w) + var(--gap) + var(--gutter));
    max-block-size: 420px;
    overflow-y: scroll;
    overflow-x: hidden;
    padding-inline-end: var(--gutter);
    box-sizing: border-box;
    scrollbar-gutter: stable both-edges;
}

.scrollArea{ scrollbar-width: thin; scrollbar-color: #7A6EF6 #1B1C21; }
.scrollArea::-webkit-scrollbar{ width: 12px; }
.scrollArea::-webkit-scrollbar-track{ background:#1B1C21; border-radius:8px; }
.scrollArea::-webkit-scrollbar-thumb{ background:#7A6EF6; border-radius:8px; }
.scrollArea::-webkit-scrollbar-thumb:hover{ background:#9A90FF; }

.list{
    inline-size: calc(var(--rail) + var(--tab-w) + var(--gap));
    list-style: none;
    margin: 0;
    padding: 0;
}

.row{
    display: grid;
    grid-template-columns: var(--rail) var(--tab-w);
    column-gap: var(--gap);
    align-items: center;
    margin-block: 8px;
}

.rowHandle{
    inline-size: var(--rail);
    block-size: var(--row-h);
    display: grid;
    place-items: center;
    cursor: grab;
    border: 0;
    background: none;
}
.rowHandle::before{
    content: "";
    inline-size: 16px;
    block-size: 24px;
    display: block;
    background:
            radial-gradient(circle 2px, var(--grip-color) 99%, transparent 0) 4px 4px / 8px 8px no-repeat,
            radial-gradient(circle 2px, var(--grip-color) 99%, transparent 0) 12px 4px / 8px 8px no-repeat,
            radial-gradient(circle 2px, var(--grip-color) 99%, transparent 0) 4px 12px / 8px 8px no-repeat,
            radial-gradient(circle 2px, var(--grip-color) 99%, transparent 0) 12px 12px / 8px 8px no-repeat,
            radial-gradient(circle 2px, var(--grip-color) 99%, transparent 0) 4px 20px / 8px 8px no-repeat,
            radial-gradient(circle 2px, var(--grip-color) 99%, transparent 0) 12px 20px / 8px 8px no-repeat;
    opacity: .9;
}

.rowTab{
    inline-size: var(--tab-w);
    min-block-size: var(--row-h);
    box-sizing: border-box;
    background: transparent;
    border: 0;
    border-radius: 0;
}

.rowTab :global(.constructor-element){
    inline-size: 100%;
}

.rowTop .rowTab,
.rowBottom .rowTab{
    margin-inline-end: var(--gutter);
}
