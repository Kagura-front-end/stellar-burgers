import { BurgerConstructorElement } from '@ui';
import type { Meta, StoryObj } from '@storybook/react';
import type { TConstructorIngredient } from '../services/constructor/constructor.slice';

const meta = {
  title: 'Example/BurgerConstructorElement',
  component: BurgerConstructorElement,
  tags: ['autodocs'],
  parameters: {
    layout: 'fullscreen',
  },
} satisfies Meta<typeof BurgerConstructorElement>;

export default meta;
type Story = StoryObj<typeof meta>;

export const DefaultElement: Story = {
  args: {
    ingredient: {
      _id: 'ing-1',
      name: 'Котлета',
      type: 'main',
      price: 100,
      image: '',
      image_large: '',
      image_mobile: '',
      proteins: 10,
      fat: 5,
      carbohydrates: 3,
      calories: 120,
      uuid: 'demo-uuid-1'
    } as TConstructorIngredient,
    index: 0,
    totalItems: 1,
    handleMoveUp: () => {},
    handleMoveDown: () => {},
    handleClose: () => {},
  },
};
